// @flow
export type DueDateFilter = 'today' | 'all';
export type CompleteStatusFilter = 'complete' | 'uncomplete' | 'all';

export const DueDateFilterEnum = {
    today: 'today',
    all: 'all'
};

export const CompleteStatusFilterEnum = {
    complete: 'complete',
    uncomplete: 'uncomplete',
    all: 'all'
};
